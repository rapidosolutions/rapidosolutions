PORT=4174

# Optional. If omitted, the blog API stores posts in server/data/blogs.json.
MONGODB_URI=

# Optional but recommended before deployment. When set, the Blog Admin page must send this key.
BLOG_ADMIN_KEY=
