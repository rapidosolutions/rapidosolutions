# Local development can leave this empty because Vite proxies /api to port 4174.
VITE_API_URL=

# On Vercel, use the public Render service URL without a trailing slash.
# VITE_API_URL=https://rapido-api.onrender.com
